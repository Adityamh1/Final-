import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maharashtra.pashubazaar',
  appName: 'Maharashtra Pashu Bazaar',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
